/**
 * What a chosen control looks like, and what an unchosen one looks like.
 *
 * The segmented control, the command filters and `Badge`'s muted tone each had their own copy, so one wrong
 * colour was three bugs — and a fork repainting `--color-primary` had to find all three.
 */

export const SELECTED = "bg-primary text-primary-foreground";
export const UNSELECTED = "bg-muted text-muted-foreground";
