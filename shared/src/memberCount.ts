/** The breakdown `/member-count` charts, as numbers both surfaces read from one count. */

export interface MemberCounts {
	total: number;
	people: number;
	bots: number;
	joinedDay: number;
	joinedWeek: number;
}
