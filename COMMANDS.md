<!-- Generated by `npx tsx scripts/generateCommandDocs.ts`. Do not edit by hand. -->

# Commands

121 commands across 14 categories.

Slash commands are shown with `/`; prefix commands use the server's configured prefix
(`t?` by default).

## Contents

- [💰 Economy](#-economy) — 23
- [🛡️ Moderation](#-moderation) — 17
- [👥 Community](#-community) — 11
- [📚 Info](#-info) — 10
- [🎮 Fun](#-fun) — 10
- [🎵 Music](#-music) — 23
- [📈 Levelling](#-levelling) — 2
- [🎯 Mini Games](#-mini-games) — 7
- [⚙️ Settings](#-settings) — 10
- [🎁 Giveaway](#-giveaway) — 1
- [🪪 Profile](#-profile) — 1
- [🔌 Integrations](#-integrations) — 3
- [👨‍💻 Developer](#-developer) — 2
- [❓ Help](#-help) — 1

## 💰 Economy

| Command        | Usage                               | Description                                                                                                     |
| -------------- | ----------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| `balance`      | `/balance`<br>`t?balance`           | Shows a wallet and bank balance.<br>**Aliases:** `bal`, `money`                                                 |
| `beg`          | `/beg`<br>`t?beg`                   | Asks strangers for spare change.                                                                                |
| `cooldowns`    | `/cooldowns`<br>`t?cooldowns`       | Shows your active economy cooldowns.<br>**Aliases:** `cd`                                                       |
| `daily`        | `/daily`<br>`t?daily`               | Claims your daily reward.                                                                                       |
| `deposit`      | `/deposit`<br>`t?deposit`           | Moves money from your wallet into the bank.<br>**Aliases:** `dep`                                               |
| `economy`      | `/economy`<br>`t?economy`           | Manages your economy account.<br>**Subcommands:** `create`, `delete`, `status`                                  |
| `economy-info` | `/economy-info`<br>`t?economy-info` | Detailed economy information.<br>**Subcommands:** `account`, `server`<br>**Aliases:** `econinfo`, `accountinfo` |
| `gamble`       | `/gamble`<br>`t?gamble`             | Bets your money on a game of chance.<br>**Aliases:** `bet`                                                      |
| `give`         | `/give`<br>`t?give`                 | Grants currency or XP to a member.<br>**Subcommands:** `currency`, `xp`                                         |
| `heist`        | `/heist`<br>`t?heist`               | Starts a group heist that other members can join.                                                               |
| `inventory`    | `/inventory`<br>`t?inventory`       | Shows what you own.<br>**Aliases:** `inv`, `items`                                                              |
| `leaderboard`  | `/leaderboard`<br>`t?leaderboard`   | Shows the server leaderboards.<br>**Subcommands:** `economy`, `levels`<br>**Aliases:** `lb`, `top`              |
| `lottery`      | `/lottery`<br>`t?lottery`           | Runs and enters the server lottery.<br>**Subcommands:** `info`, `enter`, `setup`, `freeze`, `delete`            |
| `pet`          | `/pet`<br>`t?pet`                   | Buys and looks after a pet.<br>**Subcommands:** `shop`, `buy`, `view`, `feed`, `walk`                           |
| `rehome`       | `/rehome`<br>`t?rehome`             | Rehomes your pet for half of what you paid.                                                                     |
| `reset`        | `/reset`                            | Resets economy or level data.<br>**Subcommands:** `user`, `server`, `levels`                                    |
| `rob`          | `/rob`<br>`t?rob`                   | Tries to rob another member's wallet.                                                                           |
| `shop`         | `/shop`<br>`t?shop`                 | Browses and buys from the shop.<br>**Subcommands:** `view`, `buy`, `sell`<br>**Aliases:** `store`               |
| `transfer`     | `/transfer`<br>`t?transfer`         | Sends money from your wallet to someone else.<br>**Aliases:** `pay`, `send`                                     |
| `treasure`     | `/treasure`                         | Configures random money drops in chat.<br>**Subcommands:** `configure`, `status`                                |
| `use`          | `/use`<br>`t?use`                   | Uses an item from your inventory.                                                                               |
| `withdraw`     | `/withdraw`<br>`t?withdraw`         | Moves money from the bank into your wallet.<br>**Aliases:** `with`                                              |
| `work`         | `/work`<br>`t?work`                 | Works a shift for money.                                                                                        |

## 🛡️ Moderation

| Command    | Usage                       | Description                                                                                          |
| ---------- | --------------------------- | ---------------------------------------------------------------------------------------------------- |
| `add`      | `/add`                      | Adds an emoji or sticker to the server.<br>**Subcommands:** `emoji`, `sticker`                       |
| `announce` | `/announce`                 | Posts an announcement embed in a channel.                                                            |
| `ban`      | `/ban`<br>`t?ban`           | Bans a user from the server.                                                                         |
| `clear`    | `/clear`<br>`t?clear`       | Bulk deletes recent messages in this channel.<br>**Aliases:** `purge`, `prune`                       |
| `create`   | `/create`                   | Creates embeds and threads.<br>**Subcommands:** `embed`, `thread`                                    |
| `kick`     | `/kick`<br>`t?kick`         | Kicks a member from the server.                                                                      |
| `lock`     | `/lock`<br>`t?lock`         | Stops everyone from sending messages in a channel.                                                   |
| `mute`     | `/mute`<br>`t?mute`         | Times a member out.<br>**Aliases:** `timeout`                                                        |
| `nickname` | `/nickname`<br>`t?nickname` | Changes or clears a member's nickname.<br>**Aliases:** `nick`, `setnick`                             |
| `role`     | `/role`<br>`t?role`         | Adds or removes a role from a member.<br>**Subcommands:** `add`, `remove`                            |
| `say`      | `/say`                      | Sends a message through the bot.                                                                     |
| `slowmode` | `/slowmode`<br>`t?slowmode` | Sets the slowmode for a channel.<br>**Aliases:** `slow`, `ratelimit`                                 |
| `softban`  | `/softban`                  | Temporarily bans a user, lifting the ban automatically.<br>**Subcommands:** `add`, `remove`, `list`  |
| `unban`    | `/unban`<br>`t?unban`       | Unbans a user by ID.                                                                                 |
| `unlock`   | `/unlock`<br>`t?unlock`     | Lets everyone send messages in a channel again.                                                      |
| `unmute`   | `/unmute`<br>`t?unmute`     | Removes a member's timeout.<br>**Aliases:** `untimeout`                                              |
| `warn`     | `/warn`                     | Issues and manages warnings.<br>**Subcommands:** `create`, `list`, `info`, `edit`, `remove`, `clear` |

## 👥 Community

| Command        | Usage                             | Description                                                                                   |
| -------------- | --------------------------------- | --------------------------------------------------------------------------------------------- |
| `advice`       | `/advice`<br>`t?advice`           | Gets a piece of random advice.                                                                |
| `animal-fact`  | `/animal-fact`<br>`t?animal-fact` | Shares a fact and a photo of an animal.<br>**Aliases:** `animalfact`, `animal`                |
| `calculate`    | `/calculate`<br>`t?calculate`     | Evaluates a mathematical expression.<br>**Aliases:** `calc`, `math`                           |
| `clash-royale` | `/clash-royale`                   | Looks up Clash Royale players and clans.<br>**Subcommands:** `player`, `clan`                 |
| `dbd`          | `/dbd`<br>`t?dbd`                 | Dead by Daylight perk lookups and random builds.<br>**Subcommands:** `perk`, `build`, `stats` |
| `impersonate`  | `/impersonate`                    | Sends a message through a webhook that looks like another member.                             |
| `lyrics`       | `/lyrics`<br>`t?lyrics`           | Finds the lyrics to a song.                                                                   |
| `meme`         | `/meme`<br>`t?meme`               | Fetches a random meme from Reddit.                                                            |
| `minecraft`    | `/minecraft`<br>`t?minecraft`     | Checks the status of a Minecraft server.<br>**Aliases:** `mc`                                 |
| `translate`    | `/translate`<br>`t?translate`     | Translates text into another language.                                                        |
| `wiki`         | `/wiki`<br>`t?wiki`               | Looks something up on Wikipedia.<br>**Aliases:** `wikipedia`                                  |

## 📚 Info

| Command             | Usage                                 | Description                                                                                                                  |
| ------------------- | ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| `avatar`            | `/avatar`<br>`t?avatar`               | Shows a user's avatar and banner.<br>**Aliases:** `av`, `pfp`                                                                |
| `bot`               | `/bot`<br>`t?bot`                     | Information about the bot itself.<br>**Subcommands:** `info`, `uptime`, `specs`<br>**Aliases:** `botinfo`, `about`, `uptime` |
| `bot-stats-channel` | `/bot-stats-channel`                  | Posts a self-updating bot statistics message in a channel.<br>**Subcommands:** `set`, `remove`                               |
| `member-count`      | `/member-count`<br>`t?member-count`   | Charts the member breakdown for this server.<br>**Aliases:** `membercount`, `members`                                        |
| `movie-tracker`     | `/movie-tracker`<br>`t?movie-tracker` | Looks up information about a film.<br>**Aliases:** `movie`, `film`                                                           |
| `permissions`       | `/permissions`<br>`t?permissions`     | Lists the permissions a member has in this server.<br>**Aliases:** `perms`                                                   |
| `ping`              | `/ping`<br>`t?ping`                   | Checks the bot's latency.<br>**Aliases:** `latency`, `pong`                                                                  |
| `role-info`         | `/role-info`<br>`t?role-info`         | Shows information about a role.<br>**Aliases:** `roleinfo`                                                                   |
| `server-info`       | `/server-info`<br>`t?server-info`     | Displays information about this server.<br>**Aliases:** `serverinfo`, `guildinfo`                                            |
| `user-info`         | `/user-info`<br>`t?user-info`         | Shows information about a user.<br>**Aliases:** `userinfo`, `whois`                                                          |

## 🎮 Fun

| Command         | Usage                                 | Description                                                                                                                                             |
| --------------- | ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ascii`         | `/ascii`<br>`t?ascii`                 | Converts text into ASCII art.                                                                                                                           |
| `dad-joke`      | `/dad-joke`<br>`t?dad-joke`           | Fetches a random dad joke.<br>**Aliases:** `dadjoke`, `dadj`                                                                                            |
| `fake-tweet`    | `/fake-tweet`<br>`t?fake-tweet`       | Renders a fake tweet from a user.                                                                                                                       |
| `hack`          | `/hack`<br>`t?hack`                   | Pretends to hack the mentioned user. It is entirely fake.                                                                                               |
| `how`           | `/how`<br>`t?how`                     | Calculates how much of something you are. Results are definitely accurate.<br>**Subcommands:** `gay`, `sus`, `stupid`, `simp`, `drunk`, `high`, `smart` |
| `iq`            | `/iq`<br>`t?iq`                       | Measures someone's totally scientific IQ.<br>**Aliases:** `iqtest`, `iqscore`                                                                           |
| `master-oogway` | `/master-oogway`<br>`t?master-oogway` | Shares a piece of Oogway's wisdom.                                                                                                                      |
| `nitro`         | `/nitro`<br>`t?nitro`                 | Generates a fake Nitro gift link.                                                                                                                       |
| `pepe-sign`     | `/pepe-sign`<br>`t?pepe-sign`         | Puts your text on a hand-held sign.                                                                                                                     |
| `relationship`  | `/relationship`<br>`t?relationship`   | Checks the compatibility between two people.<br>**Aliases:** `ship`, `love`                                                                             |

## 🎵 Music

| Command       | Usage                             | Description                                                                           |
| ------------- | --------------------------------- | ------------------------------------------------------------------------------------- |
| `autoplay`    | `/autoplay`<br>`t?autoplay`       | Toggles automatic related-track playback.                                             |
| `filters`     | `/filters`<br>`t?filters`         | Adds, removes or clears audio filters.<br>**Aliases:** `filter`, `fx`                 |
| `forward`     | `/forward`<br>`t?forward`         | Skips ahead in the current track.<br>**Aliases:** `ff`                                |
| `join`        | `/join`<br>`t?join`               | Brings the bot into your voice channel.<br>**Aliases:** `summon`                      |
| `leave`       | `/leave`<br>`t?leave`             | Disconnects the bot from the voice channel.                                           |
| `now-playing` | `/now-playing`<br>`t?now-playing` | Shows what is playing right now.<br>**Aliases:** `np`, `nowplaying`                   |
| `pause`       | `/pause`<br>`t?pause`             | Pauses playback.                                                                      |
| `play`        | `/play`<br>`t?play`               | Plays a track, playlist or search query.<br>**Aliases:** `p`                          |
| `play-skip`   | `/play-skip`<br>`t?play-skip`     | Plays a track immediately, skipping what is playing.<br>**Aliases:** `playskip`, `ps` |
| `play-top`    | `/play-top`<br>`t?play-top`       | Adds a track to the front of the queue.<br>**Aliases:** `playtop`, `pt`               |
| `previous`    | `/previous`<br>`t?previous`       | Goes back to the previous track.<br>**Aliases:** `prev`, `back`                       |
| `queue`       | `/queue`<br>`t?queue`             | Shows the current queue.<br>**Aliases:** `q`                                          |
| `radio`       | `/radio`<br>`t?radio`             | Plays a continuous radio station.                                                     |
| `repeat`      | `/repeat`<br>`t?repeat`           | Changes the loop mode.<br>**Aliases:** `loop`                                         |
| `resume`      | `/resume`<br>`t?resume`           | Resumes playback.<br>**Aliases:** `unpause`                                           |
| `rewind`      | `/rewind`<br>`t?rewind`           | Jumps backwards in the current track.<br>**Aliases:** `rw`                            |
| `seek`        | `/seek`<br>`t?seek`               | Jumps to a position in the current track.                                             |
| `shuffle`     | `/shuffle`<br>`t?shuffle`         | Shuffles the queue.                                                                   |
| `skip`        | `/skip`<br>`t?skip`               | Skips the current track.<br>**Aliases:** `s`, `next`                                  |
| `skip-to`     | `/skip-to`<br>`t?skip-to`         | Jumps to a specific position in the queue.<br>**Aliases:** `skipto`, `jump`           |
| `stop`        | `/stop`<br>`t?stop`               | Stops playback and clears the queue.<br>**Aliases:** `disconnect`                     |
| `tts`         | `/tts`<br>`t?tts`                 | Speaks a message in your voice channel.                                               |
| `volume`      | `/volume`<br>`t?volume`           | Sets the playback volume.<br>**Aliases:** `vol`                                       |

## 📈 Levelling

| Command     | Usage               | Description                                                                                 |
| ----------- | ------------------- | ------------------------------------------------------------------------------------------- |
| `levelling` | `/levelling`        | Configures the levelling system.<br>**Subcommands:** `setup`, `toggle`, `disable`, `status` |
| `rank`      | `/rank`<br>`t?rank` | Shows your level and rank in this server.<br>**Aliases:** `level`, `xp`                     |

## 🎯 Mini Games

| Command             | Usage                                         | Description                                                               |
| ------------------- | --------------------------------------------- | ------------------------------------------------------------------------- |
| `8ball`             | `/8ball`<br>`t?8ball`                         | Asks the magic eight ball.<br>**Aliases:** `eightball`                    |
| `blackjack`         | `/blackjack`<br>`t?blackjack`                 | Plays a hand of blackjack against the dealer.<br>**Aliases:** `bj`        |
| `fast-type`         | `/fast-type`<br>`t?fast-type`                 | Race to type a sentence correctly.<br>**Aliases:** `fasttype`, `typerace` |
| `guess-the-number`  | `/guess-the-number`<br>`t?guess-the-number`   | Guess the number the bot is thinking of.<br>**Aliases:** `gtn`            |
| `guess-the-pokemon` | `/guess-the-pokemon`<br>`t?guess-the-pokemon` | Guess the Pokémon from its artwork.<br>**Aliases:** `gtp`, `whosthat`     |
| `rps`               | `/rps`<br>`t?rps`                             | Plays rock, paper, scissors.<br>**Aliases:** `rockpaperscissors`          |
| `would-you-rather`  | `/would-you-rather`<br>`t?would-you-rather`   | Poses a would-you-rather question.<br>**Aliases:** `wyr`                  |

## ⚙️ Settings

| Command          | Usage                   | Description                                                                                                             |
| ---------------- | ----------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| `anti-link`      | `/anti-link`            | Deletes links posted by members without the bypass permission.<br>**Subcommands:** `enable`, `disable`, `status`        |
| `audit-logging`  | `/audit-logging`        | Logs server events to a channel.<br>**Subcommands:** `enable`, `disable`, `status`                                      |
| `auto-role`      | `/auto-role`            | Gives roles automatically to new members.<br>**Subcommands:** `add`, `remove`, `list`                                   |
| `automod`        | `/automod`              | Creates Discord AutoMod rules.<br>**Subcommands:** `flagged-words`, `spam`, `mention-spam`, `keyword`, `list`, `delete` |
| `counting`       | `/counting`             | Runs the counting game in a channel.<br>**Subcommands:** `setup`, `disable`, `reset`, `status`                          |
| `prefix`         | `/prefix`<br>`t?prefix` | Manages the prefix command system for this server.<br>**Subcommands:** `show`, `change`, `toggle`, `reset`              |
| `sticky-message` | `/sticky-message`       | Keeps a message pinned to the bottom of a channel.<br>**Subcommands:** `setup`, `disable`, `check`                      |
| `verify`         | `/verify`               | Configures the verification system.<br>**Subcommands:** `setup`, `disable`, `status`                                    |
| `voice-stats`    | `/voice-stats`          | Shows live member and bot counts in voice channel names.<br>**Subcommands:** `setup`, `refresh`, `status`               |
| `welcome-system` | `/welcome-system`       | Greets new members when they join.<br>**Subcommands:** `set`, `remove`, `status`                                        |

## 🎁 Giveaway

| Command    | Usage       | Description                                                            |
| ---------- | ----------- | ---------------------------------------------------------------------- |
| `giveaway` | `/giveaway` | Runs giveaways.<br>**Subcommands:** `start`, `end`, `reroll`, `delete` |

## 🪪 Profile

| Command   | Usage                     | Description                                                                      |
| --------- | ------------------------- | -------------------------------------------------------------------------------- |
| `profile` | `/profile`<br>`t?profile` | Views and edits your public profile.<br>**Subcommands:** `view`, `set`, `delete` |

## 🔌 Integrations

| Command     | Usage        | Description                                                                                                                                  |
| ----------- | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------- |
| `instagram` | `/instagram` | Watches Instagram accounts and posts their updates.<br>**Subcommands:** `watch`, `unwatch`, `list`, `latest`                                 |
| `spotify`   | `/spotify`   | Links your Spotify account and shows your listening stats.<br>**Subcommands:** `login`, `logout`, `top-tracks`, `top-artists`, `now-playing` |
| `valorant`  | `/valorant`  | Looks up Valorant agents, weapons and maps.<br>**Subcommands:** `agent`, `weapon`, `map`                                                     |

## 👨‍💻 Developer

| Command      | Usage         | Description                           |
| ------------ | ------------- | ------------------------------------- |
| `bug-report` | `/bug-report` | Reports a bug to the developers.      |
| `suggest`    | `/suggest`    | Sends a suggestion to the developers. |

## ❓ Help

| Command | Usage               | Description                                                      |
| ------- | ------------------- | ---------------------------------------------------------------- |
| `help`  | `/help`<br>`t?help` | Lists everything the bot can do.<br>**Aliases:** `commands`, `h` |
