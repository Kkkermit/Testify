/**
 * Index file for Devs tests
 * This ensures Jest discovers and runs tests in this directory
 */

describe('Devs Tests', () => {
  it('should properly load Devs tests', () => {
    expect(true).toBe(true);
  });
});
