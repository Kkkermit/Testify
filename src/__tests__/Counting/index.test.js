/**
 * Index file for Counting tests
 * This ensures Jest discovers and runs tests in this directory
 */

describe('Counting Tests', () => {
  it('should properly load Counting tests', () => {
    expect(true).toBe(true);
  });
});
