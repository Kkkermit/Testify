/**
 * Index file for Community tests
 * This ensures Jest discovers and runs tests in this directory
 */

describe('Community Tests', () => {
  it('should properly load Community tests', () => {
    expect(true).toBe(true);
  });
});
