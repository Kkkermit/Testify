---
id: server-only
title: “This command only works inside a server”
topic: troubleshooting
keywords: direct message, dm, only works in server, private message, server only
questions: Why does a command not work in DMs?
related: how-commands-work
---
Many commands act on a server, such as moderation, the economy and levelling, so they cannot run in a direct message. Run them in a channel of the server instead.

Commands that do work in DMs include `/help`, `/ask`, `/ping`, `/profile` and the fun commands.
