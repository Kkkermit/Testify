---
id: channel-controls
title: Locking channels and slowmode
topic: moderation
keywords: lock, unlock, lock channel, slowmode, slow mode, stop people talking, raid, close channel
questions: How do I lock a channel? | How do I set slowmode? | How do I stop everyone talking in a channel?
commands: lock, unlock, slowmode
related: clearing-messages
---
Both need **Manage Channels**.

### Locking
- `/lock` stops everyone from sending messages in a channel. It defaults to the one you are in, and takes an optional reason.
- `/unlock` lets them talk again.

### Slowmode
`/slowmode` sets how long each member waits between messages, such as `10s` or `5m`. Use `0` to turn it off.
